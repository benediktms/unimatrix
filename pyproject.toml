[project]
name = "unimatrix"
version = "0.1.0"
description = "Dual-platform agent framework for Claude Code and OpenCode"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"
