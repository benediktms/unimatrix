#!/usr/bin/env bash
# Auto-reinstall unimatrix after every commit
REPO_DIR="$(git rev-parse --show-toplevel)"
echo "unimatrix: syncing installation..."
if "$REPO_DIR/install.sh" --global > /dev/null 2>&1; then
  echo "unimatrix: sync complete."
else
  echo "unimatrix: sync failed!" >&2
fi
